---
domains:
  canonical:
    - "{{ DOMAIN_PRIMARY }}"
  aliases: []
networks:
  local:
    subnet: 192.168.105.32/28
  overlay:
    modes: [swarm]
    topology: shared_net
    collect_proxy_resolvable: true
    consumer:
      kind: web_facing
