# TODO

- [ ] Wire Zammad's LDAP source configuration end-to-end (V3 variant): add a post-bootstrap rails-runner script analogous to `apply_oidc_settings.sh` that creates the `LdapSource` record against `svc-db-openldap` and verifies user-attribute sync. Currently V3 deploys cleanly, but LDAP authentication is configured manually via the Zammad admin UI; an end-to-end Playwright test for LDAP login is also missing.
- [ ] Configure SMTP outbound and IMAP inbound channels in Zammad via post-bootstrap rails-runner. Currently V1 deploys cleanly with Mailu present, but Zammad's "Email" channels still need to be configured manually. Once auto-wired, add a smoke test that emails the `helpdesk@{{ DOMAIN_PRIMARY }}` mailbox and asserts a ticket is created within 60s.
- [ ] Implement Keycloak group → Zammad role mapping. Zammad's OmniAuth-OpenIDConnect strategy CAN consume group claims if a Keycloak protocol mapper is added to the shared client, but role-side mapping requires custom Rails code (Zammad doesn't ship a built-in claim-to-role translator). Until implemented, all OIDC logins default to `Customer`. See [022 Decision #5](../../docs/requirements/022-web-app-zammad.md).
- [ ] Add a Playwright assertion that the server-name alias declared in `meta/server.yml` serves the canonical vhost (not a 301 redirect). Currently the alias is configured at the `meta/server.yml` level; sys-svc-proxy handles aliasing centrally.
- [ ] Sweep this role to consume a central `svc-db-elasticsearch` role once it exists; today Elasticsearch is bundled in the role's compose stack.
