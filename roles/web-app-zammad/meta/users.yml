---
administrator: {}
helpdesk:
  roles:
    - mail-bot
zammad-api-bot:
  username: zammad-api-bot
  firstname: Infinito
  lastname: API Bot
  description: API-only Basic-auth bot used by the Playwright suite (NOT an OIDC user)
zammad-wizard-bypass-admin:
  username: wizard-bypass-admin
  firstname: Wizard
  lastname: Bypass
  description: Local-password admin seeded by Zammad's auto_wizard.json to skip the setup wizard (NOT used at runtime)
