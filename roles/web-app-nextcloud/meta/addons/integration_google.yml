---
enabled: "{{ lookup('api_enabled', 'google') }}"
required: true
mechanism: plugin
source: upstream
config:
  plugin_configuration:
    - appid: "integration_google"
      configkey: "client_id"
      configvalue: "{{ lookup('api', 'google.client_id') }}"
    - appid: "integration_google"
      configkey: "client_secret"
      configvalue: "{{ lookup('api', 'google.client_secret') }}"
