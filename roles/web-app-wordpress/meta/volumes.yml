---
data:
  type: volume
  name: wordpress_data
  mounts:
    - service: wordpress
      target: "/var/www/html"
msmtprc:
  type: bind
  source: "{{ [ lookup('container', 'web-app-wordpress', 'directories.config'), 'msmtprc.conf'] | path_join }}"
  read_only: true
  mounts:
    - service: wordpress
      target: /etc/msmtprc
