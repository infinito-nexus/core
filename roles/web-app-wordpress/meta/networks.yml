---
local:
  subnet: 192.168.63.0/24
overlay:
  modes: [compose, swarm]
  topology: shared_net
  consumer:
    kind: mcp_client
  proxy_resolvable: true
  proxy_aliases: ["{{ lookup('domain', 'web-app-wordpress') }}"]
