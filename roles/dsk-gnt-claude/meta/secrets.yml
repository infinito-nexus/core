---
credentials:
  gateway_key:
    description: Virtual key the gateway minted for this workstation. The gateway deploys from a different inventory than the workstation, so the operator
      carries the key over by hand.
    algorithm: plain
    validation: ^.*$
