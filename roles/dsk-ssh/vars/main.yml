---
application_id: dsk-ssh
system_service_id: "{{ application_id }}"
desk_ssh_repository: "{{ lookup('config', application_id, 'services.ssh.repository') }}"
desk_ssh_legacy_unit: "{{ [ DIR_SYSTEM_SERVICE_USER, 'ssh-agent.service' ] | path_join }}"
