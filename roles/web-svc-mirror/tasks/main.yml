---
- include_tasks: 01_core.yml
  when: run_once_web_svc_mirror is not defined
  vars:
    application_id: "web-svc-mirror"
    webserver_vhost_flavour: "basic"

- name: "Reset compose Handler for Mirror"
  include_tasks: utils/load_handlers.yml
  vars:
    handler_role_name: "sys-svc-compose"
