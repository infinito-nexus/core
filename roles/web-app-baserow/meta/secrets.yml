---
credentials:
  secret_key:
    description: Django SECRET_KEY for Baserow
    algorithm: alphanumeric
    validation:
      min_length: 50
  mcp_endpoint_key:
    description: URL-path secret of the MCP endpoint, stored in MCPEndpoint.key (varchar 32)
    algorithm: random_hex_16
    validation: "^[a-f0-9]{32}$"
