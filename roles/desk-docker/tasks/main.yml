---
- name: 📦 install docker
  package:
    name:
      - docker
      - docker-compose
    state: present

- name: "🔧 Add user {{ lookup('users', WORKSTATION_USER).username }} to docker group"
  user:
    name: "{{ lookup('users', WORKSTATION_USER).username }}"
    groups: docker
    append: true
