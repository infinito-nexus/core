---
credentials:
  api_key:
    description: "Bearer key the jeff classifier accepts; the gateway's router hook is its only consumer"
    algorithm: "alphanumeric"
    validation:
      min_length: 32
