---
- name: "🧩 load docker, db and proxy for {{ application_id }}"
  include_role:
    name: sys-stk-full
  vars:
    compose_handlers_flush: true

- name: "🔧 Fider | OIDC setup"
  include_tasks: 01_setup_oidc.yml
  when:
    - FIDER_OIDC_ENABLED | bool
    - IS_STACK_HOST | bool
