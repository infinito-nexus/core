---
config:
  type: config
  source: "{{ [ lookup('container', 'web-app-listmonk', 'directories.config'), 'config.toml' ] | path_join }}"
  mode: "0440"
  mounts:
    - service: listmonk
      target: /listmonk/config.toml
