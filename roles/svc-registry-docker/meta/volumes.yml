# nocheck: backup-service-flag locally built images are rebuildable; the volume is backup-disabled and the consumer permanently off
---
data:
  type: volume
  name: docker_registry_data
  mounts:
    - service: docker
      target: /var/lib/registry
