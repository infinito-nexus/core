---
credentials:
  administrator_password:
    description: Initial password of the Home Assistant owner account created during headless onboarding
    algorithm: alphanumeric
    validation:
      min_length: 32
  mcp_token:
    description: Long-lived access token issued to MCP clients of this hub
    algorithm: alphanumeric
    validation:
      min_length: 50
