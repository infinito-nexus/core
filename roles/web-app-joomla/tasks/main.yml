---
- name: "📂 Include role sys-stk-front-proxy for {{ application_id }}"
  include_role:
    name: sys-stk-front-proxy
  loop: "{{ JOOMLA_DOMAINS }}"
  loop_control:
    loop_var: domain
  vars:
    http_port: "{{ lookup('config', application_id, 'services.joomla.ports.local.http') }}"
    webserver_client_max_body_size: "{{ JOOMLA_POST_MAX_SIZE }}"

- name: "🧩 load docker and db for {{ application_id }}"
  include_role:
    name: sys-stk-backend
  vars:
    compose_handlers_flush: false

- name: 📂 Include PHP Config tasks
  include_tasks: 01_config.yml

- name: 🚿 flush docker service
  meta: flush_handlers

- name: "👔 Joomla manager-side operations (install, assert, reset admin, OIDC plugin)"
  include_tasks: 02_manager_ops.yml
  when: IS_STACK_HOST | bool
