---
# nocheck: info-media Self-written web-svc glue role: configures an Nginx vhost to serve static HTML over HTTPS with Let's Encrypt. Nginx/Let's Encrypt are dependencies, not the product this role represents; no consumer-facing upstream product website.
