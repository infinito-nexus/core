---
- include_tasks: 00_core.yml
  when: run_once_svc_db_mariadb is not defined
  vars:
    application_id: svc-db-mariadb

- name: "🎬 Initialize dedicated database"
  include_tasks: 01_init.yml
  when: database_init | bool
