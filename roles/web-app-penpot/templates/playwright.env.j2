ADMIN_PASSWORD={{ lookup('users', 'administrator').password | dotenv_quote }}

ADMIN_USERNAME={{ lookup('users', 'administrator').username | dotenv_quote }}

APP_BASE_URL={{ lookup('tls', application_id, 'url.base') | dotenv_quote }}

# nocheck: playwright-service-gate — asset CDN not deployed for this role
ASSET_SERVICE_ENABLED={{ lookup('config', application_id, 'services.asset.enabled', false) | string | lower }}

BIBER_PASSWORD={{ lookup('users', 'biber').password | dotenv_quote }}

BIBER_USERNAME={{ lookup('users', 'biber').username | dotenv_quote }}

CANONICAL_DOMAIN={{ lookup('domain', application_id) | dotenv_quote }}

# nocheck: playwright-service-gate — CDN not deployed for this role
CDN_SERVICE_ENABLED={{ lookup('config', application_id, 'services.cdn.enabled', false) | string | lower }}

CSS_BASE_URL={{ lookup('tls', 'web-svc-css', 'url.base') | default('') | dotenv_quote }}

# nocheck: playwright-service-gate — consumed by assertCspInjections in shared persona (INJECTOR_SERVICES dynamic iteration)
CSS_SERVICE_ENABLED={{ lookup('config', application_id, 'services.css.enabled') | string | lower }}

# nocheck: playwright-service-gate — email relay is backend-only, not tested in E2E spec
EMAIL_SERVICE_ENABLED={{ lookup('config', application_id, 'services.email.enabled') | string | lower }}

JAVASCRIPT_BASE_URL=''

# nocheck: playwright-service-gate — consumed by assertCspInjections in shared persona (INJECTOR_SERVICES dynamic iteration)
JAVASCRIPT_SERVICE_ENABLED={{ lookup('config', application_id, 'services.javascript.enabled') | string | lower }}

# nocheck: playwright-service-gate — LDAP disabled by default; integration tested via admin API not E2E
LDAP_SERVICE_ENABLED={{ lookup('config', application_id, 'services.ldap.enabled') | string | lower }}

LOGOUT_SERVICE_ENABLED={{ lookup('config', application_id, 'services.logout.enabled') | string | lower }}

MATOMO_BASE_URL={{ lookup('tls', 'web-app-matomo', 'url.base') | default('') | dotenv_quote }}

MATOMO_SERVICE_ENABLED={{ lookup('config', application_id, 'services.matomo.enabled') | string | lower }}

# Penpot uses its own in-app OIDC plugin, not oauth2-proxy
OAUTH2_SERVICE_ENABLED=false

OIDC_SERVICE_ENABLED={{ lookup('config', application_id, 'services.oidc.enabled') | string | lower }}

# nocheck: playwright-service-gate — DB engine presence; covered by role-local integration tests
POSTGRES_SERVICE_ENABLED={{ lookup('config', application_id, 'services.postgres.enabled') | string | lower }}

# nocheck: playwright-service-gate — simpleicons not deployed for this role
SIMPLEICONS_SERVICE_ENABLED={{ lookup('config', application_id, 'services.simpleicons.enabled', false) | string | lower }}
