{% include 'roles/sys-svc-compose/templates/base.yml.j2' %}

  {% set service_name = 'build' %}
  {% set docker_restart_policy = 'no' %}
  {{ service_name }}:
    {{ lookup('container_image', application_id, 'build') }}
    {% include 'roles/sys-svc-container/templates/base.yml.j2' %}
    command: ["sh", "{{ lookup('volume', application_id, 'cdn_build_script').mounts[0].target }}"]
    {{ lookup('container_networks') | indent(4) }}
    {{ lookup('container_volumes', service_name) | indent(4) }}

{{ lookup('compose_volumes') }}
{{ lookup('compose_networks') }}
