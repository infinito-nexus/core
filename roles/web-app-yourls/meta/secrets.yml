---
credentials:
  administrator_password:
    description: Initial password for the YOURLS administrator account
    algorithm: sha256
    validation: ^[a-f0-9]{64}$
  cookie_key:
    description: Salt YOURLS derives its login cookie and its form nonces from
    algorithm: sha256
    validation: ^[a-f0-9]{64}$
