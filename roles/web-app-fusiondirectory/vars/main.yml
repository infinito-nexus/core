---
application_id: web-app-fusiondirectory
container_port: 80
entity_name: "{{ application_id | get_entity_name }}"

FUSIONDIRECTORY_CONTAINER: "{{ lookup('config', application_id, 'services.fusiondirectory.name') }}"
