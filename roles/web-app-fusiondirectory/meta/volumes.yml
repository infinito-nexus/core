---
# nocheck: embedded-state-on-nfs generated asset cache; the authoritative state lives in LDAP
data:
  type: volume
  name: fusiondirectory_data
  mounts:
    - service: fusiondirectory
      target: /assets/fusiondirectory
