---
# nocheck: playwright-service-flag
hermes:
  modes:
    compose:
      enabled: true
    swarm:
      enabled: true
  bond: 1
  enabled: true
  shared: true
  backup:
    no_stop_required: true
  image: nousresearch/hermes-agent
  version: "v2026.7.20"
  name: hermes
  min_storage: 1146MB
  cpus: "2.0"
  mem_reservation: 2g
  mem_limit: 4g
  pids_limit: 2048
  ports:
    local:
      http: 8089
      sso: 16512
    internal:
      http: 8642
  run_after:
    - web-app-homeassistant
  lifecycle: beta
  proxy:
    service_key: hermes

# nocheck: playwright-service-flag, dynamic-flag
kata:
  bond: 1
  enabled: true
  shared: true

sso:
  bond: 1
  enabled: "{{ 'web-app-keycloak' in group_names }}"
  shared: "{{ 'web-app-keycloak' in group_names }}"
  flavor: oauth2
  oauth2:
    origin:
      host: hermes
      port: 8642
    acl:
      whitelist:
        - /v1/
        - /health
    allowed_groups:
      - "{{ lookup('rbac_group_path', application_id='web-app-hermes', role='administrator') }}"
      - "{{ lookup('rbac_group_path', application_id='web-app-hermes', role='mcp') }}"

# nocheck: logout, dynamic-flag, playwright-service-flag
logout:
  bond: 1
  enabled: false
  shared: false

# nocheck: playwright-service-flag
litellm:
  bond: 1
  enabled: "{{ 'svc-ai-litellm' in group_names }}"
  shared: "{{ 'svc-ai-litellm' in group_names }}"

dashboard:
  bond: 1
  enabled: "{{ 'web-app-dashboard' in group_names }}"
  shared: "{{ 'web-app-dashboard' in group_names }}"
matomo:
  bond: 1
  enabled: "{{ 'web-app-matomo' in group_names }}"
  shared: "{{ 'web-app-matomo' in group_names }}"
css:
  bond: 1
  enabled: "{{ 'web-svc-css' in group_names }}"
  shared: "{{ 'web-svc-css' in group_names }}"
prometheus:
  bond: 1
  enabled: "{{ 'web-app-prometheus' in group_names }}"
  shared: "{{ 'web-app-prometheus' in group_names }}"

# nocheck: playwright-service-flag, dynamic-flag
container_backup: # nocheck: bond-baseline
  bond: 1
  enabled: "{{ 'svc-bkp-volume-2-local' in group_names }}"
  shared: true

# nocheck: playwright-service-flag
homeassistant:
  bond: 1
  enabled: "{{ 'web-app-homeassistant' in group_names }}"
  shared: "{{ 'web-app-homeassistant' in group_names }}"
