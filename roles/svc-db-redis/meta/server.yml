---
networks:
  local:
    subnet: 192.168.207.0/24
  overlay:
    modes: [compose, swarm]
    topology: shared_net
    aliases: [redis-central]
    consumer:
      kind: services_flags
      key: redis
