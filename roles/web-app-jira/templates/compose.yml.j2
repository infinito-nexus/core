{% include 'roles/sys-svc-compose/templates/base.yml.j2' %}
  {% set service_name = 'jira' %}
  {{ service_name }}:
    {{ lookup('template', 'roles/sys-svc-container/templates/build.yml.j2') | indent(4) }}
      args:
        JIRA_IMAGE: "{{ JIRA_IMAGE }}"
        JIRA_VERSION: "{{ JIRA_VERSION }}"
        JIRA_STORAGE_PATH: "{{ JIRA_STORAGE_PATH }}"
    {{ lookup('container_image', application_id, 'jira') }}
    {{ lookup('compose_only', 'container_name', JIRA_CONTAINER) }}
    hostname: '{{ JIRA_HOSTNAME }}'
    {{ lookup('container_ports', ['jira', 'http', DOCKER_BIND_HOST]) | indent(4) }}
    {{ lookup('container_volumes', service_name) | indent(4) }}
    {{ lookup('container_healthcheck', service_name) | indent(4) }}
    {% include 'roles/sys-svc-container/templates/base.yml.j2' %}
    {{ lookup('container_depends_on', application_id) | indent(4) }}
    {{ lookup('container_networks') | indent(4) }}

{{ lookup('compose_volumes') }}
{{ lookup('compose_networks') }}
