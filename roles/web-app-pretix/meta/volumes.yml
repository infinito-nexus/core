---
data:
  type: volume
  name: pretix_data
  mounts:
    - service: pretix
      target: /data
config:
  type: volume
  name: pretix_config
  mounts:
    - service: pretix
      target: /etc/pretix
