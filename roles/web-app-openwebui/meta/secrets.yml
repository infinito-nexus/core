---
credentials:
  webui_secret_key:
    description: "Signing key Open WebUI requires whenever authentication is enabled. Pinned by the deployment so a container exec sees the same value as the server,
      which auto-generates it into the process environment only."
    algorithm: alphanumeric
    validation: "^[A-Za-z0-9]{32,}$"
    length: 64
  comfyui_api_key:
    description: Key of the image-generation backend the operator points this deployment at. Empty unless services.openwebui.comfyui_url names one, because the
      platform ships no pinned ComfyUI image of its own.
    algorithm: plain
    validation: ^.*$
  litellm_api_key:
    description: Per-consumer virtual key for the shared svc-ai-litellm gateway
    algorithm: sk_prefixed
    validation:
      min_length: 50
