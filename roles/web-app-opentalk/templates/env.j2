{% include 'roles/sys-svc-container/templates/env.j2' %}

RUST_LOG={{ 'debug' if MODE_DEBUG | bool else 'info' }}

CONTROLLER_HOST={{ OPENTALK_DOMAIN }}
INSECURE={{ (not (lookup('tls', application_id, 'enabled') | bool)) | string | lower }}
BASE_URL=

{% if OPENTALK_OIDC_ENABLED | bool %}
OIDC_ISSUER={{ OIDC.CLIENT.ISSUER_URL }}
OIDC_CLIENT_ID={{ OIDC.CLIENT.ID }}
{% endif %}
{% if OPENTALK_OPENCLOUD_ENABLED | bool %}
SHARED_FOLDER_OPENCLOUD_URL={{ OPENTALK_OPENCLOUD_URL }}
{% endif %}
