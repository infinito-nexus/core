---
credentials:
  auth_token:
    description: Token a client presents to the kernel gateway. Anything holding it can execute code inside the sandbox, so it is not shared with browser users.
    algorithm: random_hex_32
    validation: non_empty_string
