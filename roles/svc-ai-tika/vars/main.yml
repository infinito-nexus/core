---
application_id: "svc-ai-tika"
entity_name: "{{ application_id | get_entity_name }}"

TIKA_CONTAINER: "{{ lookup('config', application_id, 'services.tika.name') }}"
TIKA_PORT: "{{ lookup('config', application_id, 'services.tika.ports.internal.http') }}"
