{% include 'roles/sys-svc-compose/templates/base.yml.j2' %}

  {% set service_name = 'minio' %}
  {{ service_name }}:
    {% include 'roles/sys-svc-container/templates/base.yml.j2' %}
    {{ lookup('container_image', application_id, 'minio') }}
    {{ lookup('compose_only', 'container_name', MINIO_CONTAINER) }}
    {{ lookup('container_ports',
              ['api', 'http', DOCKER_BIND_HOST],
              ['console', 'http', DOCKER_BIND_HOST]) | indent(4) }}
    command: server /data --console-address ":{{ MINIO_CONSOLE_PORT_INTERNAL }}"
    {{ lookup('container_volumes', application_id, service_name) | indent(4) }}
    {{ lookup('container_networks') | indent(4) }}

{{ lookup('compose_networks') }}

{{ lookup('compose_volumes', application_id) }}
