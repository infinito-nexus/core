---
local:
  subnet: 192.168.206.0/24
overlay:
  modes: [compose, swarm]
  topology: shared_net
  consumer:
    key: seaweedfs
  proxy_resolvable: true
  proxy_aliases: ["{{ lookup('config', 'web-app-seaweedfs', 'domains.canonical.api') }}"]
