---
- name: "🧩 load docker, db and proxy for {{ application_id }}"
  include_role:
    name: sys-stk-full
  vars:
    compose_handlers_flush: true

- name: "👔 Snipe-IT manager-side operations (container exec)"
  include_tasks: 01a_manager_ops.yml
  when: IS_STACK_HOST | bool
