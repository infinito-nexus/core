---
- include_tasks: utils/once/flag.yml

- name: "🛡️ Select the isolating runtime for {{ application_id }}"
  ansible.builtin.include_tasks: "{{ lookup('path_absolute', 'roles/svc-virt-kata/tasks/utils/select_runtime.yml') }}"

- name: "🧩 load docker and db for {{ application_id }}"
  include_role:
    name: sys-stk-full

- name: "📄 Render the OpenClaw gateway config (local + swarm peers)"
  ansible.builtin.include_tasks:
    file: "{{ lookup('path_absolute', 'roles/sys-svc-compose/tasks/utils/render_replicated_templates.yml') }}"
  vars:
    role_templates:
      - src: "openclaw.json.j2"
        dest: "{{ OPENCLAW_CONFIG_PATH_HOST }}"
