---
credentials:
  litellm_api_key:
    description: Per-consumer virtual key for the shared svc-ai-litellm gateway
    algorithm: sk_prefixed
    validation:
      min_length: 50
  gateway_token:
    description: OpenClaw gateway token guarding the control UI and API
    algorithm: alphanumeric
    validation:
      min_length: 32
