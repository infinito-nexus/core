---
# nocheck: dynamic-flag  the desktop base is the premise of every dsk role, not a per-host option
desktop-base:
  bond: 1
  enabled: true
  shared: true
laya:
  modes:
    host:
      enabled: true
  lifecycle: pre-alpha
  min_storage: "1GB"
  cpus: "2.0"
  mem_reservation: "512m"
  mem_limit: "4g"
  pids_limit: 1024
