---
# Matrix-deploy variant overrides for web-svc-css.
# See docs/contributing/artefact/files/role/variants.md for the file format.

- services:
    prometheus:
      enabled: true
      shared: true
    tor:
      enabled: true
- services:
    prometheus:
      enabled: false
      shared: false
    tor:
      enabled: false
      shared: false # nocheck: variants-static-override
