---
- name: 📦 install hunspell
  package_install:
    id: hunspell

- name: 📦 install hunspell language packages
  package: # nocheck: dynamic-package
    name: "hunspell-{{ item }}"
    state: present
  loop: "{{ hunspell_languages }}"
