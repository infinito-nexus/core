---
pds_data:
  type: volume
  mounts:
    - service: pds
      target: /opt/pds

pdsadmin:
  type: config
  source: "{{ [lookup('container', 'web-app-bluesky', 'directories.volumes'), 'pdsadmin', 'pdsadmin'] | path_join }}"
  mode: "0555"
  mounts:
    - service: pds
      target: /usr/local/bin/pdsadmin
