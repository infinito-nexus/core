{% include 'roles/sys-svc-compose/templates/base.yml.j2' %}
  {{ CHESS_SERVICE }}:
    {{ lookup('container_image', application_id, 'chess') }}
    {{ lookup('compose_only', 'container_name', CHESS_CONTAINER) }}
    hostname: "{{ CHESS_HOSTNAME }}"
    {{ lookup('container_ports', ['chess', 'http', DOCKER_BIND_HOST]) | indent(4) }}
    {{ lookup('container_volumes', application_id, CHESS_SERVICE) | indent(4) }}
{% include 'roles/sys-svc-container/templates/healthcheck/curl.yml.j2' %}
{% include 'roles/sys-svc-container/templates/base.yml.j2' %}
    {{ lookup('container_depends_on', application_id) | indent(4) }}
    {{ lookup('container_networks') | indent(4) }}

{{ lookup('compose_volumes', application_id) }}
{{ lookup('compose_networks') }}
