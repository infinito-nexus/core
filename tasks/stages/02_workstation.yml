---
- name: 🔧 setup workstation user
  include_role:
    name: "user-workstation"

- name: "🔧 setup docker role includes for workstations"
  include_tasks: ./tasks/utils/setup/group.yml
  loop: "{{ lookup('stage_groups', 'workstation') }}"
  loop_control:
    loop_var: role_group
    label: "{{ role_group }}"

- name: 🖥️ general host setup
  include_role:
    name: drv-non-free
