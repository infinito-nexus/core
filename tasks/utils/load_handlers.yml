---
- name: "📂 load pure handlers from '{{ handler_role_name }}' (no 'when') to force run"
  import_role:
    name: "{{ handler_role_name }}"
    tasks_from: "none.yml"
    handlers_from: "main"
  vars:
    application_id: "{{ handler_role_name }}"
