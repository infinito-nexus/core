# Empty on purpose: Fedora uses dnf, which reaches the package-cache
# through `yum-fedora`, not through apt. The bind mount is unconditional
# in the compose overrides, and docker would create a host directory
# here if the source were missing.
